// Library exports

export { cn, formatAddress, formatETH, formatNumber, truncateText } from './utils';
export {
  config,
  supportedChains,
  REQUIRED_CHAIN,
  getChainById,
  isSupportedChain,
  isCorrectChain,
  CONTRACT_ADDRESSES,
  getContractAddress,
  RPC_URLS,
  EXPLORER_URLS,
  getExplorerUrl,
  NATIVE_SYMBOLS,
  getNativeSymbol,
} from './wagmi';
